{
  "name": "@fast-window/app-v5-reference-app-go",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:backend": "node scripts/build-backend.mjs",
    "build:resources": "node scripts/build-resources.mjs",
    "build:ui": "pnpm build:backend && vite build && pnpm build:resources",
    "build:exe:dev": "pnpm build:ui && cargo build --manifest-path src-tauri/Cargo.toml",
    "build:exe": "pnpm build:ui && pnpm tauri build",
    "dev": "vite --host 127.0.0.1 --port 1434",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
