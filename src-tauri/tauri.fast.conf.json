{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "pnpm run build && pnpm exec vite preview --port 1420 --strictPort --host 127.0.0.1",
    "devUrl": "http://127.0.0.1:1420"
  }
}
