# 更新日志

本文件记录 Fast Window 宿主可感知的行为变更。所有条目均使用中文描述。

## 未发布

### 新增

- AI Once 应用设置新增模型服务等待秒数，默认等待调整为 600 秒，问答和模型列表刷新会按用户保存的等待时间执行。
- 宿主新增独立的能力登记簿记录，用户选取的应用能力会单独保存并显示在主页搜索列表中。
- 收藏集新增“添加收藏”能力，外部使用方可选择片区和分组后，将文件夹、网址或文件添加到收藏集指定位置。
- Quick Bar 新增临时选择注册方式，按钮可先注册能力本身，再在结果窗里现场选择参数后执行，原有直接注册方式保持不变。
- 宿主应用商店按应用类别分区展示：桌面应用保持在上方区域，服务应用独立分区显示在下方；没有服务应用时下分区自动隐藏。

### 调整

- 拆分注册应用的宿主快捷命令与 App 能力 API：应用注册页只管理宿主快捷命令，能力登记簿只管理可调用能力，主页会用不同角标区分两类按钮。
- 应用注册页新增“读取宿主快捷命令”能力；当 App 未运行时会提示是否启动后读取，并将结果写回注册页内的宿主快捷命令列表。
- 统一宿主侧运行时声明通路：宿主快捷命令与 App 能力 API 分别读取，普通 App 动作成功响应不再夹带声明清单。
- 统一宿主侧能力返回文字协议：应用能力调用必须主动返回标准展示文字，宿主、Quick Bar 与主窗口能力入口不再识别旧式回答。
- 统一宿主侧能力配置选项整理：能力配置会提供整理后的标准清单，Quick Bar 与主窗口能力入口不再分别拆解原始返回。
- 优化主窗口唤醒快捷键行为：主窗口已出现但未聚焦时，首次按快捷键会优先聚焦窗口；只有主窗口已经处于焦点状态时，再次按快捷键才会隐藏窗口。
- 优化注册 App 窗口模式快捷键行为：窗口已运行但不在前台时，首次按快捷键会优先找回窗口；只有窗口已经在前台时，再次按快捷键才会隐藏窗口。
- 应用升级改为逐个文件覆盖进 `package`：新包不包含的内容保持不动，覆盖失败自动完整回滚，不再整体替换 `package` 目录。
- 应用声明消灭双轨制：所有应用统一为单份 `fw-app.json`（身份、展示与运行声明），构建规则移入 `fw-app.build.json`；打包链路只认这一种清单形态，旧版 `fw-app.package.json` 全面废弃。
- 注册表改为“读时最新、写时迁移”：宿主向前端提供应用列表时按应用路径实时读取最新 `fw-app.json` 动态展示，注册表只持久化用户配置（快捷键、自启、窗口记忆、自定义名称/图标/显示模式与命令覆盖），下一次写入时自动完成结构切换。
- 宿主应用商店不再显示旧版插件下载区域：旧版插件的远程安装与后台自动更新机制一并退役；已安装的旧插件继续可用、停留在当前版本，本地导入插件不受影响。
- 仓库退役插件开发发布工具链：不再提供插件构建/监听/发布命令，宿主 dev 启动不再自动编译插件；`plugins/` 源码保留作为参考，插件仍可通过本地导入安装与运行。
- eucli-studio 客户端独立化：客户端整体迁移进应用仓库统一体系，应用清单、构建打包与中央发布链路与其他应用对齐，客户端后台解除对 eucli-box 仓库的构建期依赖；旧的初步复制版与开发期数据清除。
- 仓库退役应用开发发布旧工具链：根部旧的应用打包/升版/发布命令与工具移除，应用开发动作统一由应用内部工具与中央协议体系承担；中央协议工具新增应用独立校验与下架能力，宿主发布链改用宿主自有支撑模块，行为不变。
- 应用协议工具支持商店化产出落点与形态选择：产出目录可自定义（相对协议目录或绝对路径），形态可选压缩包或散装（散装把程序、清单与图标直接铺进本地实例目录）；独立校验默认读取产出区最新成品，清单比对按语义归一（空白项、服务声明、图标包内基准），不再按固定命名与字面逐字比对。
- 标题栏刷新键升级为整页刷新：一次重扫插件、重读已注册应用（图标、版本与宿主快捷命令随最新构建实时更新）并对账运行状态，替代原先只刷新插件的行为。
