# Fast Window 发布凭据
#
# 原则：两个真实仓库，对应两个发布 Token。不要复用同一把 Token。
# - 分发仓 Token：负责插件、v5 app、catalog.json 元数据。
# - 宿主仓 Token：负责宿主 MSI Release 与安装包资产。

# 分发仓发布 Token
# 目标仓库：noelle-silva/fast-window-plugins-download
# 使用范围：插件发布、v5 app 发布、catalog.json 元数据写入。
# GitHub Fine-grained Token 最小权限：Contents = Read and write。
FAST_WINDOW_GITHUB_TOKEN=

# 宿主 MSI 发布 Token
# 目标仓库：noelle-silva/fast-window
# 使用范围：创建/更新宿主 MSI GitHub Release，并上传 MSI 资产。
# GitHub Fine-grained Token 最小权限：Contents = Read and write。
FAST_WINDOW_HOST_RELEASE_GITHUB_TOKEN=
