# Google Lens（本地图片）

目标：在 Fast Window 里快速把“本地图片”送到 Google Lens 的上传入口。

注意：由于浏览器/安全限制，插件无法把图片“自动填入” Lens 的文件选择框；需要在 Lens 网页里手动上传或粘贴。

## 用法

1. 在插件里粘贴（Ctrl+V）/拖拽/选择一张图片（可选：用于复制/下载）。
2. 点击“打开 Lens 上传页”。
3. 在 Lens 页面里选择文件上传，或尝试 Ctrl+V 粘贴（取决于网页支持）。

